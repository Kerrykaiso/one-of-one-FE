"use client";
import { FormEvent, useState } from "react";
import Link from "next/link";
import { Check } from "lucide-react";
export function AccountForm({signup=false}:{signup?:boolean}){const [done,setDone]=useState(false);if(done)return <Success text={signup?"Your account has been created.":"Welcome back. This demo does not connect to a real account."}/>;return <form className="simple-form" onSubmit={(e)=>{e.preventDefault();setDone(true)}}>{signup&&<label>Full name<input required/></label>}<label>Email address<input type="email" required/></label><label>Password<input type="password" required minLength={8}/></label>{signup&&<label>Confirm password<input type="password" required minLength={8}/></label>}{!signup&&<div className="form-options"><label><input type="checkbox"/> Remember me</label><button type="button">Forgot password?</button></div>}<button className="button button-dark">{signup?"Create account":"Sign in"} <span>↗</span></button><p>{signup?"Already have an account?":"New to One of One?"} <Link href={signup?"/login":"/signup"}>{signup?"Sign in":"Create account"}</Link></p><hr/><p>Are you an artist? <Link href="/artist/signup">Apply here ↗</Link></p></form>}
export function ContactForm(){const [done,setDone]=useState(false);if(done)return <Success text="Your note is in our hands. We’ll reply within two working days."/>;return <form className="simple-form contact-form" onSubmit={(e:FormEvent)=>{e.preventDefault();setDone(true)}}><div><label>Name<input required/></label><label>Email<input type="email" required/></label></div><label>Subject<select required defaultValue=""><option value="" disabled>Select a topic</option><option>Order support</option><option>Artist application</option><option>Press & partnerships</option><option>Something else</option></select></label><label>Message<textarea rows={6} required/></label><button className="button button-dark">Send message <span>↗</span></button></form>}
function Success({text}:{text:string}){return <div className="form-success"><Check/><h3>ALL SET.</h3><p>{text}</p></div>}
